# overview.md — ai_agent_orchestration

## What this module does
LLM-driven Teacher Agent: lesson planner, explainer agent, questioner agent, evaluator agent, adaptation controller — implemented as a multi-agent / state-machine pipeline (Understand->Plan->Explain->Demonstrate->Question->Evaluate->Adapt->Continue).

## Reads before coding
1. Root `instructions/Contract.md`
2. Root `02_Architecture.md`
3. This folder's `detail_plan.md`
